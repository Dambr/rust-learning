для ввода с клавиатуры необходимо подключить модуль ввода-вывода: use std::io
читать из консоли: io::stdin().read_line(&mut arg); // arg - переменная, куда записать результат пользовательского ввода

i16 и u8 складывать нельзя

требуется приведение типа через изспользование as

++ в rust нет

// Ссылки
Может быть только одна изменяемая ссылка и любое количество неи
Нельзя создать изменяемую ссылку если существуют неизменяемые
